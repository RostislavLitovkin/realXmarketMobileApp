<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XcavateMobileApp.Pages.ModifyUserProfilePopupView"
             xmlns:card="clr-namespace:PlutoFramework.Components.Card;assembly=PlutoFramework"
             xmlns:buttons="clr-namespace:PlutoFramework.Components.Buttons;assembly=PlutoFramework"
             xmlns:form="clr-namespace:PlutoFramework.Components.Form;assembly=PlutoFramework"
             IsVisible="{Binding IsVisible}"
             AutomationId="ModifyUserProfilePopup"
             AbsoluteLayout.LayoutBounds="0.5, 0.5, 1, 1"
             AbsoluteLayout.LayoutFlags="All">

    <card:BottomPopupCard Title="Enter your information"
                          IsShown="{Binding IsVisible}">
        <card:BottomPopupCard.View>
            <AbsoluteLayout>
                <VerticalStackLayout Spacing="15"
                                     AbsoluteLayout.LayoutBounds="0, 0, 1, AutoSize"
                                     AbsoluteLayout.LayoutFlags="WidthProportional"
                                     x:Name="verticalStackLayout"
                                     Padding="10, 10, 10, 10"
                     >

                    <form:FormInputView Placeholder="First name"
                        AutomationId="ModifyProfileFirstNameInput"
                        Text="{Binding FirstName}" />

                    <form:FormInputView Placeholder="Last name"
                        AutomationId="ModifyProfileLastNameInput"
                        Text="{Binding LastName}" />

                    <form:FormInputView Placeholder="Email"
                        AutomationId="ModifyProfileEmailInput"
                        ValidateEmail="True"
                        Text="{Binding Email}" />

                    <form:FormInputView Placeholder="Phone Number"
                        AutomationId="ModifyProfilePhoneInput"
                        Text="{Binding PhoneNumber}"/>
                </VerticalStackLayout>
                
                <AbsoluteLayout AbsoluteLayout.LayoutBounds="0.5, 1, 1, 80"
                    AbsoluteLayout.LayoutFlags="PositionProportional, WidthProportional">

                    <Grid AbsoluteLayout.LayoutBounds="0.5, 0.5, 1, 50"
                      AbsoluteLayout.LayoutFlags="PositionProportional, WidthProportional"
                      ColumnSpacing="15"
                      Margin="10, 0, 10, 10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <buttons:ElevatedButton Text="Continue"
                                                AutomationId="ModifyProfileContinueButton"
                                                Grid.Column="0"
                                                Command="{Binding ContinueCommand}"
                                                ButtonState="{Binding SaveButtonState}"/>
                    </Grid>
                </AbsoluteLayout>

            </AbsoluteLayout>

        </card:BottomPopupCard.View>
    </card:BottomPopupCard>
</ContentView>
